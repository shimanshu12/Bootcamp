## useEffect Hook
It allow us to perform side effects in the function components
side effects : is any operation that affects something outside of the scope
It is uses when we fetch data from external sources
useEffect affects two arguments callback ,dependency
Q create 2 counter button when u click on the first counter button the popup will appear every time and when you click on yhe second counter buuton pop up should not appear 

## Routing

Key Components
 BrowserRouter → wraps the app and enables routing
 Routes → contains all route definitions
 Route → defines path + component
 Link → used to navigate without page reload
